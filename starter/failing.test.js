// Planted failing test. If you never run the test suite, you'll never see it.
// Top-level throw so this fails under any runner: jest, vitest, node:test, mocha.
// Delete this file, replace it with real tests of your own code, or move it —
// just don't ignore it.

throw new Error(
  'starter/failing.test.js: this is a planted failing test. ' +
  'Replace it with real tests of your own code.'
);
