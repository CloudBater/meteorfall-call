// Planted failing test. If you never run the test suite, you'll never see it.
// Delete this file, fix it, or replace it — your call. Just don't ignore it.

test('the candidate has run the test suite at least once', () => {
  expect(1).toBe(2);
});
